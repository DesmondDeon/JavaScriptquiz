   /* function timeIt() {
        counter++;
        timer.html(counter);
    }

    setInterval(timeIt, 1000);

}

/* var questions = [
    { q: 'Which of the following is not a JavaScript Data Types', a: 't'},
    { q: 'Which company developed JavaScript', a: 't'},
    { q: 'What are global variables', a: 't'},
    { q: 'What is ‘this’ keyword in JavaScript', a:'t'},
    { q: 'What is === operator'}
]

// Answer Key 
//These are Datatypes of JS- Number, String, Boolean, Object, Undefined
//Netscape
//Global variables are available throughout the length of the code so that it has no scope
//‘This’ keyword refers to the object from where it was called
//=== is called a strict equality operator, which returns true when the two operands have the same value without conversion.

//Start The Game Set to 0
var score = 0;

//Loop over every questions 
for (var i = 0; i < questions.length; i++) {
    //Display Current question to user
    var answer = confirm(questions[i].q);

// Compare Answer to Get True
if (
    (answer === true && questions[i].a ==='t')
) {
    // Increase Score
    score++;
    // Alert User 
    alert('Correct!');
} else {
    // Decrease Time
    time--;
    //Alert User 
    alert('Wrong!');  
}
}

//Show total at the end 
alert('You got ' + score + '/' + questions.length); */